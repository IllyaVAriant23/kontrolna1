#include <stdio.h>
#include <math.h>

int main() {
    double x, y, а;

    printf("Введіть значення x: ");
    scanf("%lf", &x);

    printf("Введіть значення y: ");
    scanf("%lf", &y);

    а = pow(x, 2) - pow(y, 2);

    printf("Результат: %.2lf\n", а);

    return 0;
}
